services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: smart
      POSTGRES_PASSWORD: smart_password
      POSTGRES_DB: smart_interaction
    ports:
      - "5432:5432"
    volumes:
      - smart_postgres:/var/lib/postgresql/data

volumes:
  smart_postgres:
