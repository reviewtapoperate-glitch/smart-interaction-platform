import { API, apiPath } from "/api-contract.js";
const tokenKey="reviewtap_token"; const state={endpoints:[]}; const $=id=>document.getElementById(id); const escapeHtml=value=>String(value??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c])); function show(message,error=false){const node=$("message");node.textContent=message;node.style.color=error?"#b42318":"#047857";} async function request(route,{params,body}={}){const headers={"Content-Type":"application/json"};if(route.auth==="bearer")headers.Authorization=`Bearer ${localStorage.getItem(tokenKey)||""}`;const r=await fetch(apiPath(route,params),{method:route.method,headers,body:body===undefined?undefined:JSON.stringify(body)});if(r.status===401){localStorage.removeItem(tokenKey);location.assign("/");throw new Error("Your session has ended");}const t=await r.text();let p={};try{p=t?JSON.parse(t):{};}catch{p={error:t};}if(!r.ok)throw new Error(p.error||"Request failed");return p;}
function renderList(id,rows,empty,formatter){$(id).innerHTML=rows.length?rows.map(formatter).join(""):`<p>${empty}</p>`;} function render(data){const{business,summary,endpoints,products,branches,sessions,orders,payments,staff,events,integrations}=data;$("welcome").textContent=`${business.name} on ReviewTap`;$("businessName").value=business.name||"";$("websiteUrl").value=business.websiteUrl||"";$("googleBusinessUrl").value=business.googleBusinessUrl||"";$("currency").value=business.currency||"KES";$("timezone").value=business.timezone||"Africa/Nairobi";$("stats").innerHTML=[["Live endpoints",summary.endpoints],["Sessions",summary.sessions],["Orders",summary.orders],["Interactions",summary.interactionEvents],["Revenue",`${business.currency} ${(summary.paidAmountMinor/100).toFixed(2)}`]].map(([l,v])=>`<article class="price"><div class="eyebrow">${l}</div><div class="amount">${v}</div></article>`).join("");state.endpoints=endpoints;renderList("endpoints",endpoints,"No endpoints yet.",e=>`<article class="price portal-row"><b>${escapeHtml(e.name)}</b> · ${escapeHtml(e.type)} · ${escapeHtml(e.status)}<br><small>${escapeHtml(e.code)}</small><br><a href="${escapeHtml(e.publicUrl)}" target="_blank">Open smart link</a> <button class="secondary" data-qr="${e.id}">QR</button> <button class="secondary" data-destination="${e.id}">Destinations</button></article>`);renderList("products",products,"No products yet.",p=>`<div class="price portal-row"><b>${escapeHtml(p.name)}</b> — ${(p.priceMinor/100).toFixed(2)} ${escapeHtml(p.currency)} ${p.active?"":"(inactive)"}</div>`);renderList("branches",branches,"No locations yet.",b=>`<div class="price portal-row"><b>${escapeHtml(b.name)}</b> — ${escapeHtml(b.code)} ${b.address?`· ${escapeHtml(b.address)}`:""}</div>`);renderList("sessions",sessions,"No sessions yet.",s=>`<div class="portal-row"><b>${escapeHtml(s.endpoint?.name||"Endpoint")}</b><br>${s.status} · ${new Date(s.createdAt).toLocaleString()}</div>`);renderList("orders",orders,"No orders yet.",o=>`<div class="portal-row"><b>${o.status}</b><br>${o.items?.length||0} items · ${new Date(o.createdAt).toLocaleString()}</div>`);renderList("payments",payments,"No payments yet.",p=>`<div class="portal-row"><b>${p.status}</b><br>${escapeHtml(p.provider)} · ${(p.amountMinor/100).toFixed(2)} ${escapeHtml(p.currency)}</div>`);renderList("staff",staff,"No staff yet.",s=>`<div class="portal-row"><b>${escapeHtml(s.name)}</b><br>${escapeHtml(s.email)} · ${escapeHtml(s.role)}</div>`);renderList("events",events,"No events yet.",e=>`<div class="portal-row"><b>${escapeHtml(e.name)}</b><br>${e.status} · ${new Date(e.startsAt).toLocaleString()}</div>`);renderList("integrations",integrations,"No integrations yet.",i=>`<div class="portal-row"><b>${escapeHtml(i.provider)}</b><br>${escapeHtml(i.type)} · ${i.active?"Connected":"Not connected"}</div>`);document.querySelectorAll("[data-qr]").forEach(b=>b.addEventListener("click",()=>downloadQr(b.dataset.qr)));document.querySelectorAll("[data-destination]").forEach(b=>b.addEventListener("click",()=>editDestinations(b.dataset.destination)));}
async function load(){try{const[business,summary,endpoints,products,branches,sessions,orders,payments,staff,events,integrations,onboarding,quotes]=await Promise.all([request(API.business.get),request(API.analytics.summary),request(API.endpoints.list),request(API.products.list),request(API.business.branches),request(API.business.sessions),request(API.business.orders),request(API.business.payments),request(API.business.staff),request(API.events.list),request(API.integrations.list),request(API.onboarding.get),request(API.onboarding.quotes)]);render({business,summary,endpoints,products,branches,sessions,orders,payments,staff,events,integrations});renderOnboarding(onboarding,quotes);}catch(e){show(e.message,true);}}
function renderOnboarding(config,quotes){if(config){$("businessType").value=config.businessType||"Restaurant";const ops=config.operations||{};document.querySelectorAll("[data-op]").forEach(c=>c.checked=Boolean(ops[c.dataset.op]));const payments=config.payments||[];document.querySelectorAll("#paymentChoices input").forEach(c=>c.checked=payments.includes(c.value));const integrations=config.integrations||[];document.querySelectorAll("#integrationChoices input").forEach(c=>c.checked=integrations.includes(c.value));$("nfcRequired").checked=Boolean(config.hardware?.nfc);$("qrRequired").checked=config.hardware?.qr!==false;$("hardwareQuantity").value=config.hardware?.quantity||"";$("locationList").value=(config.locations||[]).join(", ");}$("quotes").innerHTML=quotes.length?quotes.map(q=>`<article class="price portal-row"><b>${q.status}</b> · ${escapeHtml(q.currency)} ${(q.setupMinor/100).toFixed(2)} setup · ${escapeHtml(q.currency)} ${(q.monthlyMinor/100).toFixed(2)}/month<br>${escapeHtml(q.notes||"")} ${q.status==="SENT"?`<button class="primary" data-accept-quote="${q.id}">Accept quote</button>`:""}</article>`).join(""):"<p>No quote yet. Submit your configuration and the ReviewTap team can prepare one.</p>";document.querySelectorAll("[data-accept-quote]").forEach(b=>b.addEventListener("click",async()=>{try{await request(API.onboarding.acceptQuote,{params:{id:b.dataset.acceptQuote},body:{}});show("Quote accepted.");load();}catch(e){show(e.message,true);}}));}
async function editDestinations(id){const e=state.endpoints.find(x=>x.id===id);if(!e)return;const c=e.actionProfile||{};const reviewUrl=prompt("Google review URL",c.reviewUrl||"");if(reviewUrl===null)return;const whatsappUrl=prompt("WhatsApp URL",c.whatsappUrl||"");if(whatsappUrl===null)return;const websiteUrl=prompt("Website URL",c.websiteUrl||"");if(websiteUrl===null)return;try{await request(API.endpoints.update,{params:{id},body:{actionProfile:{...c,reviewUrl:reviewUrl||null,whatsappUrl:whatsappUrl||null,websiteUrl:websiteUrl||null}}});show("Destinations updated.");load();}catch(e){show(e.message,true);}} async function downloadQr(id){try{const r=await fetch(apiPath(API.endpoints.qr,{id}),{headers:{Authorization:`Bearer ${localStorage.getItem(tokenKey)||""}`}});if(!r.ok)throw new Error("Unable to create QR image");const url=URL.createObjectURL(await r.blob());const a=document.createElement("a");a.href=url;a.download=`reviewtap-${id}.png`;a.click();URL.revokeObjectURL(url);}catch(e){show(e.message,true);}}
$("businessForm").addEventListener("submit",async e=>{e.preventDefault();try{await request(API.business.update,{body:{name:$("businessName").value,websiteUrl:$("websiteUrl").value||null,googleBusinessUrl:$("googleBusinessUrl").value||null,currency:$("currency").value||"KES",timezone:$("timezone").value||"Africa/Nairobi"}});show("Business saved.");load();}catch(x){show(x.message,true);}});$("configForm").addEventListener("submit",async e=>{e.preventDefault();const operations={};document.querySelectorAll("[data-op]").forEach(c=>operations[c.dataset.op]=c.checked);const payments=[...document.querySelectorAll("#paymentChoices input:checked")].map(c=>c.value);const integrations=[...document.querySelectorAll("#integrationChoices input:checked")].map(c=>c.value);try{await request(API.onboarding.save,{body:{businessType:$("businessType").value,operations,payments,integrations,hardware:{nfc:$("nfcRequired").checked,qr:$("qrRequired").checked,quantity:Number($("hardwareQuantity").value||0)},locations:$("locationList").value.split(",").map(x=>x.trim()).filter(Boolean),staff:{quantity:0,roles:[]}}});show("Configuration submitted for quote.");load();}catch(x){show(x.message,true);}});$("endpointForm").addEventListener("submit",async e=>{e.preventDefault();try{await request(API.endpoints.create,{body:{name:$("endpointName").value,code:$("endpointCode").value,type:$("endpointType").value,actionProfile:{allowOrdering:$("allowOrdering").checked}}});e.target.reset();show("Endpoint created.");load();}catch(x){show(x.message,true);}});$("productForm").addEventListener("submit",async e=>{e.preventDefault();try{await request(API.products.create,{body:{name:$("productName").value,price:Number($("productPrice").value)}});e.target.reset();show("Product added.");load();}catch(x){show(x.message,true);}});$("branchForm").addEventListener("submit",async e=>{e.preventDefault();try{await request(API.business.createBranch,{body:{name:$("branchName").value,code:$("branchCode").value,address:$("branchAddress").value||undefined}});e.target.reset();show("Location added.");load();}catch(x){show(x.message,true);}});$("logout").addEventListener("click",()=>{localStorage.removeItem(tokenKey);location.assign("/");});load();
